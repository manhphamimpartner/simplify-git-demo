{
  "name": "simplify-git-demo",
  "version": "1.2.0",
  "description": "Demo project for clean git history talk",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "lint": "eslint src/"
  },
  "engines": { "node": ">=16" }
}
